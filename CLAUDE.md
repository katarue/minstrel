# CLAUDE.md - Minstrel

このファイルは Minstrel プロジェクト固有のルールを定義します。

共通ルール（コミュニケーション、4原則、Git運用等）は `C:\Users\katar\CLAUDE.md` を参照してください。このファイルには Minstrel 固有の内容のみを記載します。

---

## プロジェクト概要

**プロジェクト名**: Minstrel  
**サブタイトル**: Game Music Concert Portal  
**ドメイン**: minstrel.live  
**ステータス**: 計画完了、実装フェーズ0〜1着手段階

日本国内のゲーム音楽コンサート情報を網羅的に収集・掲載する専門ポータルサイト。情報の自動収集、機械検証によるルールベース判定、検索・フィルタ機能、カレンダー連携、X自動投稿までを含むフルオートメーション運用が前提。

---

## 必読ドキュメント

作業開始前に以下を必ず読んでください。

1. `docs/project_plan.md` - プロジェクト計画書本体（戦略・設計・哲学）
2. `docs/implementation_schedule.md` - フェーズ別実装スケジュール
3. このファイル（CLAUDE.md）

---

## 技術スタック

### フロントエンド
- Next.js 14（App Router）
- Tailwind CSS
- Vercel（Hobby無料枠）

### データベース・ストレージ
- Supabase（PostgreSQL、500MB無料枠）
- Supabase Storage（画像はホットリンク禁止、自前保存）

### 情報収集パイプライン
- Prefect（既存環境を活用、自宅PC `katarue` で稼働中）
- Python
- BeautifulSoup（HTMLサニタイズ）

### AI処理
- Claude API（Anthropic）
- 用途：構造化抽出、要約、Vision解釈
- **重要原則**: AIは抽出役、最終判定はルールベース機械検証

### X連携
- 収集: twitterapi.io（従量課金、$0.15/1,000ツイート）
- 投稿: X公式API v2無料枠
- 戦略: 個別投稿ではなく週次まとめ投稿

---

## Minstrel固有の設計原則

### 自動化の哲学
完全自動化＝「全部自動公開する」ではなく、**「人間が見なくても事故らない仕組み」**。

### 情報源ランク制
- **Aランク**（公式サイト・公式X・公式チケット販売）→ 自動公開対象
- **Bランク**（2083WEB・noteまとめ等）→ 補助情報・手動確認推奨
- **Cランク**（一般ユーザー投稿等）→ 発見用、公開ソースにしない

### 機械検証
AIによる「ファクトチェック」ではなく、ルールベースの「機械検証」を行う。条件を満たした場合のみ `auto_publish_eligible=true` とする。

### 70点の網羅性、95点の安全性
取りこぼしを許容する。間違った情報を載せるくらいなら載せない。

---

## 著作権・法務対応（重要）

- 公式サイト・チケットサイトの画像をホットリンクしない
- 画像は Supabase Storage に自前保存してから配信
- description は AI 要約 100〜200 字。原文コピー禁止
- 全イベント詳細ページに「公式情報を見る」リンクを必須配置
- 権利者からの削除要請に即時対応できる体制を維持

---

## ブランド・命名

- **ブランド名**: Minstrel（単数形、固定）
- **サブタイトル**: Game Music Concert Portal（英語固定）
- **ドメイン**: minstrel.live

---

## 実装スケジュールへの参照

実装は `docs/implementation_schedule.md` のフェーズ・タスク番号に従って進める。

タスク番号の例: `1-A-1`（フェーズ1、Aブロック、タスク1）

ムーチョさんからの指示は以下の形式が想定される。

> 「実装スケジュールの1-A-1を実装してください。詳細は計画書の5章を参照してください。」

このとき:
- 計画書の該当章を読み込んで設計意図を理解する
- 不明点があれば実装前に質問する
- 完了後は何をしたか簡潔に報告する

---

## 進捗管理

- 各タスクの完了時に簡潔な報告
- 重要な意思決定や設計変更は計画書（`docs/project_plan.md`）またはスケジュール（`docs/implementation_schedule.md`）に反映
- 実装中の発見・課題はコミットメッセージか別途ログに記録

---

## このファイルの更新

Minstrel 固有のルール・設計が変わった場合、このファイルを更新する。共通ルールはホームディレクトリの CLAUDE.md を更新する。